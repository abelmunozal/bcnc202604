apiVersion: v1
kind: Service
metadata:
  name: {{ include "prices-service.fullname" . }}
  labels:
    {{- include "prices-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prices-service.selectorLabels" . | nindent 4 }}
